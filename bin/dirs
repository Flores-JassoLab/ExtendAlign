#!/bin/execlineb -S0
multisubstitute {
	importas -u ALIGNED_AND_UNALIGNED ALIGNED_AND_UNALIGNED
	importas -u BEST_BLAST_ALIGNMENT BEST_BLAST_ALIGNMENT
	importas -u BLAST_OUTPUT BLAST_OUTPUT
	importas -u CORRECT_MISMATCHES CORRECT_MISMATCHES
	importas -u EXTENDED_ALIGNMENT EXTENDED_ALIGNMENT
	importas -u FULLY_ALIGNED FULLY_ALIGNED
	importas -u INCORRECT_MISMATCH INCORRECT_MISMATCH
	importas -u INPUT_FILES INPUT_FILES
	importas -u JOINT_STRANDS JOINT_STRANDS
	importas -u MINUS_STRAND_REVERSE_COMPLEMENT MINUS_STRAND_REVERSE_COMPLEMENT
	importas -u MINUS_STRAND_SEQUENCES MINUS_STRAND_SEQUENCES
	importas -u NO_HEADER NO_HEADER
	importas -u QUERY_LENGTH QUERY_LENGTH
	importas -u REQUERY_MINUS_STRAND REQUERY_MINUS_STRAND
	importas -u SPLIT_STRANDS SPLIT_STRANDS
	importas -u SUBJECT_LENGTH SUBJECT_LENGTH
}
${@}
