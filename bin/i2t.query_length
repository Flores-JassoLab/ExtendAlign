#!/bin/execlineb -P
multisubstitute {
	importas	${QUERY_LENGTH}	${QUERY_LENGTH}
	importas	${QUERY_FASTA}	${QUERY_FASTA}
}
pipeline { change-path	${QUERY_FASTA}	${QUERY_LENGTH} }
pipeline { change-ext	".fna"	".txt" }
change-ext	".fa"	".txt"
