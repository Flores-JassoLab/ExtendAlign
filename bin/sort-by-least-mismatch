#!/bin/execlineb -S0
multisubstitute {
	define length 1
	define mismatch 2
}
pipeline {
	add-least-mismatch-for-sort "${@}"
}
pipeline {
	sort
		-k ${length},${length}nr
		-k ${mismatch},${mismatch}n
}
remove-least-mismatch-for-sort
